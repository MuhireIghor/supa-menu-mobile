const signUp = async ({ username, email, password, phoneNumber }: { username: string, email: string, password: string, phoneNumber: string }) => {
    try {
const {data} = await api
    }
    catch (err: any) {
        throw new Error(err.message)
    }

}